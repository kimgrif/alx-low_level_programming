#include "main.h"

/**
 *mul - multiplys two numbers
 *@a: character is to be multiplied
 *@b: character is to be multiplied
 *Return:  (Success)
*/
int mul(int a, int b)
{
int c = a * b;
return (c);
}
