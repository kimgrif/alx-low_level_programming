#include "main.h"

/**
* _abs - computes the absolute value of an integer 'n
* @n: The number to compute the absolute value for
* Return: Always 'n' or '-n'
*/

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else
{
return (-n);
}
}
