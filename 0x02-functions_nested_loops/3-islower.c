#include "main.h"

/**
 * main - check the code.
 *
 *int _islower - Returns 1 if c is lowercase
 *
 * Return: Always 0.
 */


int _islower(int c)
{
char x;
if (x == 'c')
{
return (1);
}
return (0);
}
