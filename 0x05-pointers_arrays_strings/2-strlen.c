#include "main.h"
#include <string.h>

/**
 * _strlen -  returns the length of a string
 * @s: Pointer to the integer being checked
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
int len = strlen(s);

return (len);

}
